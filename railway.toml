[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "python main.py"
healthcheckPath = "/health"
healthcheckTimeout = 100
healthcheckNumRetries = 10
overlapSeconds = 0
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
